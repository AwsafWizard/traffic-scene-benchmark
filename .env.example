# Only the providers you actually use need a key.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Cloud sync (Transfer page). Both people paste the SAME two values.
# Create a free project at https://supabase.com -> Project Settings -> API
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional, defaults to traffic-bench
SUPABASE_BUCKET=
